- Allocation size now works.

- How to pass Cowns into lambda in schedule_lambda?

    - Have the Cowns be captured as a part of the lambda.

        return std::make_tuple(
            count,
            r,
            [f = std::move(f), cown_tuple = std::move(cown_tuple)]() mutable {
                /// Effectively converts ActualCown<T>... to
                /// acquired_cown... .
                auto lift_f = [f = std::move(f)](Args... args) mutable {
                std::move(f)(access_to_acquired<typename Args::Type>(args)...);
                };

                std::apply(std::move(lift_f), std::move(cown_tuple));
            });

    - This only works when the template knows how many args you have.
    - Probably need to specialize for nargs.

- No meeting next week (22nd), but on one after that.
- I've cancelled 6th may for travel.